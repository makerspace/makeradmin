upstream webshop_server {
    server webshop:80;
}

server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;

    root /var/www/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /shop/ {
        proxy_pass http://webshop_server;
    }
    
    location /member/ {
        proxy_pass http://webshop_server;
    }
}
