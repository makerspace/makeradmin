var config = {
	apiBasePath: "http://" + window.location.hostname + ":8010",
    oauthBasePath: "http://" + window.location.hostname + ":8010",
	apiVersion: "1.0",
	pagination: {
		pageSize: 25,
	},
}
